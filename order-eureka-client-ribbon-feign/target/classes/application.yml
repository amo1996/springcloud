
eureka:
  client:
    service-url:
     defaultZone: http://localhost:8761/eureka
spring:
  application:
    name: order-provider
server:
  port: 8083
#  为feign添加回退方法
feign:
  hystrix:
    enabled: true